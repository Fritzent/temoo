name: tere_mood
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  # State Management
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  
  # Navigation
  go_router: ^13.0.0
  url_strategy: ^0.2.0
  
  # Utilities
  intl: ^0.20.2
  google_fonts: ^6.1.0
  
  # UI/Assets
  flutter_svg: ^2.0.9
  firebase_core: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.11.1

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/

  generate: true
